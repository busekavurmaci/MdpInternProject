
@{
    ViewBag.Title = "Portal";
}

@*<div class="container">
    <div class="row">
        <div class="col-md-12">*@

            <div class="card text-white bg-dark mb-3" style="max-width: 18rem; margin:auto;">
                <div class="card-header">Mdp Group</div>
                <div class="card-body">
                    <h5 style="text-align:center;" class="card-title">
                        @if (Session["UserName"] != null)
                        {
                            @Session["UserName"].ToString()
                        }
                    </h5>
                    <br />
                    <p style="text-align:center;" class="card-text">MDP Group e-Dönüşüm portalına hoşgeldiniz!</p>
                </div>
            </div>

        @*</div>
    </div>
</div>*@